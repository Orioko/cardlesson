# Обновление системы авторизации - Релиз

## Что нового

### ✅ Полноценная система регистрации и авторизации

**Регистрация:**
- Теперь для входа необходимо сначала зарегистрироваться
- Проверка на существование email
- Регистрация создает уникальный профиль пользователя

**Авторизация:**
- Вход только для зарегистрированных пользователей
- Проверка правильности email и пароля
- Сохранение сессии в localStorage

### ✅ Валидация данных

**Email:**
- Проверка на корректный формат
- Обязательное заполнение

**Пароль:**
- Минимальная длина: 6 символов
- Обязательное заполнение

**Локализация:**
- Все сообщения об ошибках переведены на 3 языка:
  - 🇷🇺 Русский
  - 🇬🇧 Английский
  - 🇰🇷 Корейский

### ✅ Персональные словари

**Для каждого пользователя:**
- Отдельное хранилище слов
- Слова привязаны к userId
- При входе отображаются только ваши слова
- Разные пользователи не видят слова друг друга

**Хранение данных:**
- Все данные хранятся в localStorage браузера
- Автоматическая синхронизация с сервером
- Кэширование для быстрой загрузки

### ✅ Улучшенная архитектура кода

**Новые хуки:**
- `useAuth` - логика авторизации
- `useWords` - загрузка слов
- `useWordActions` - операции со словами

**Новые утилиты:**
- `validation.ts` - валидация данных
- `userStorage.ts` - работа с пользователями

**Преимущества:**
- Чище и проще код
- Легче поддерживать
- Удобнее добавлять новые функции

## Как использовать

### Первый запуск:

1. Откройте приложение
2. Нажмите "Нет аккаунта?"
3. Введите email (например: user@example.com)
4. Введите пароль (минимум 6 символов)
5. Нажмите "Регистрация"
6. Вы автоматически войдете в систему

### Последующие входы:

1. Откройте приложение
2. Введите ваш email
3. Введите ваш пароль
4. Нажмите "Войти"

### Работа со словами:

1. После входа добавляйте слова через "Добавить новое слово"
2. Все слова сохраняются в вашем профиле
3. При следующем входе вы увидите свои сохраненные слова
4. Каждый пользователь имеет свой личный словарь

## Технические детали

### Файлы изменений:

**Новые файлы:**
- `src/utils/validation.ts` - валидация
- `src/utils/userStorage.ts` - хранилище пользователей
- `src/hooks/useAuth.ts` - хук авторизации
- `src/hooks/useWords.ts` - хук слов
- `src/hooks/useWordActions.ts` - хук действий
- `src/hooks/index.ts` - экспорт хуков

**Обновленные файлы:**
- `src/utils/localAuth.ts` - добавлена проверка учетных данных
- `src/pages/LoginPage/LoginPage.tsx` - использует useAuth
- `src/pages/MainPage/MainPage.tsx` - использует useWords
- `src/pages/DictionaryPage/DictionaryPage.tsx` - использует хуки
- Все файлы переводов (ru/en/ko)

### localStorage структура:

```
local_user                    - текущий пользователь
registered_users              - все пользователи
words_cache_{userId}          - слова пользователя
words_cache_timestamp_{userId} - время синхронизации
```

## Миграция данных

Если у вас были слова до обновления:
- Старые слова будут доступны первому зарегистрированному пользователю
- Кэш автоматически адаптируется под новую систему
- Рекомендуется зарегистрироваться с тем же email

## Поддержка

При возникновении проблем проверьте:
1. localStorage доступен в браузере
2. Email введен в правильном формате
3. Пароль содержит минимум 6 символов

## Дальнейшие улучшения

В планах:
- Восстановление пароля
- Изменение пароля
- Удаление аккаунта
- Экспорт/импорт словаря
- Статистика обучения

