import type { Lang } from './types';

/**
 * Массив всех поддерживаемых языков в приложении.
 * Используется для итерации по языкам и фильтрации доступных переводов.
 */
export const LANGS: Lang[] = ['ru', 'en', 'ko'];
